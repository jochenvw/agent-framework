# Copy this file to .env and fill in your values.
# The sample auto-loads .env on startup via godotenv.

# ── Option A: Direct OpenAI ──────────────────────────────────────────
OPENAI_API_KEY=sk-...

# ── Option B: Azure AI Foundry with API Key ──────────────────────────
# AZURE_FOUNDRY_ENDPOINT=https://<project>.cognitiveservices.azure.com/openai/v1
# AZURE_FOUNDRY_KEY=<your-key>
# AZURE_FOUNDRY_MODEL=gpt-4o

# ── Option C: Azure AI Foundry with Azure AD (no key needed) ─────────
# AZURE_FOUNDRY_ENDPOINT=https://<project>.cognitiveservices.azure.com/openai/v1
# AZURE_FOUNDRY_MODEL=gpt-4o
# Uses DefaultAzureCredential (az login, managed identity, environment variables, etc.)

# ── Debug Options ────────────────────────────────────────────────────
# DEBUG=1    # Enable debug logging to see token acquisition, request details, etc.
